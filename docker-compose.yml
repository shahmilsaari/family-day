services:
  postgres:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: family_day
    ports:
      - "5432:5432"
    volumes:
      - family_day_pgdata:/var/lib/postgresql/data

volumes:
  family_day_pgdata:

